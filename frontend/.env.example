# Leave NEXT_PUBLIC_API_URL unset in production: the browser hits /api/* on
# the same origin and Next.js rewrites it to the internal backend service.
# Override only when you want the browser to bypass the proxy:
# NEXT_PUBLIC_API_URL=http://localhost:8000
